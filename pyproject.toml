[project]
name = "mpower-pmec-reconciliation"
version = "0.1.0"
description = "MPower PMEC payment reconciliation Streamlit app"
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.35.0",
    "pandas>=2.0.0",
    "openpyxl>=3.1.0",
]

[tool.uv]
dev-dependencies = []
